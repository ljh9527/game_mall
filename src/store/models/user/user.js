import { system as systemServices } from '-/services';
import { requestErrorHandler } from '-/utils';

const searchInitParams = {

};

const user = {
  state: {
    
  },
  reducers: {
    
  },
  effects: {
    
  },
};

export default user;
